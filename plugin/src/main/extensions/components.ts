import type { ExtensionHandler } from "./types";

/**
 * Component and component-set tools.
 *
 * Add a tool by adding one entry here. Set `edit` to true when the handler
 * writes to the file.
 */
export const componentsHandlers = {} satisfies Record<string, ExtensionHandler>;
